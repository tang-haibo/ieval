import {EvalScript} from './eval';
import {ImportScript, getResource} from './imports';

export {
  ImportScript,
  EvalScript,
  getResource,
};

export default {
  ImportScript,
  EvalScript,
  getResource,
};